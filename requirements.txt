# Core scientific stack
numpy>=1.24,<2.1
pandas>=2.0,<2.3          # 3.x breaks reading the DCRNN fixed-format metr-la.h5
scipy>=1.10
scikit-learn>=1.3
pyyaml>=6.0
tqdm>=4.65
h5py>=3.8
tables>=3.8            # pandas HDF5 backend for metr-la.h5
requests>=2.31
gdown>=5.1            # optional Google-Drive mirror fallback for datasets

# Deep learning (CPU wheels resolve automatically in Codespaces)
torch>=2.1

# Geospatial / road network
osmnx>=1.9
networkx>=3.1
geopandas>=0.14
shapely>=2.0

# Visualisation
matplotlib>=3.7
seaborn>=0.12
folium>=0.15

# Dashboard
streamlit>=1.30

# Markdown tables (pandas.DataFrame.to_markdown)
tabulate>=0.9

# Testing
pytest>=7.4

# SUMO python bindings (the SUMO binaries themselves are installed by
# .devcontainer/setup.sh via apt; this package only adds libsumo/traci helpers)
eclipse-sumo>=1.19 ; platform_system != "Windows"
traci>=1.19 ; platform_system != "Windows"
